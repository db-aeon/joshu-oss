/**
 * AGPL stub — hourly Hermes learning GitHub sync is fleet-only (private joshu repo).
 * Runtime import in hermesApi.ts is satisfied here so `tsc` and self-host images build cleanly.
 */
export async function syncHermesLearningGitCron(): Promise<"skipped"> {
  return "skipped";
}
