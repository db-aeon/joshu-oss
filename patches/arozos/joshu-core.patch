diff --git a/.gitignore b/.gitignore
index 0f6a7244..8f210393 100644
--- a/.gitignore
+++ b/.gitignore
@@ -31,3 +31,4 @@ src/system/bridge.json
 src/system/cron.json
 src/system/smtp_conf.json
 /src/web/FFmpeg Factory
+.DS_Store
diff --git a/src/setting.go b/src/setting.go
index 5b6e1194..24f772f9 100644
--- a/src/setting.go
+++ b/src/setting.go
@@ -111,6 +111,12 @@ func system_setting_getSettingGroups() []settingGroup {
 			IconPath: "SystemAO/system_setting/img/info.svg",
 			Desc:     "Information of the current running ArOZ Online System",
 		},
+		{
+			Name:     "Joshu",
+			Group:    "Joshu",
+			IconPath: "img/joshu/chat.png",
+			Desc:     "Joshu box snapshots and factory profile",
+		},
 	}
 }
 
diff --git a/src/setting_joshu.go b/src/setting_joshu.go
new file mode 100644
index 00000000..17a58677
--- /dev/null
+++ b/src/setting_joshu.go
@@ -0,0 +1,24 @@
+package main
+
+/*
+	Joshu System Setting pages (box snapshots, factory profile).
+*/
+
+func JoshuSettingInit() {
+	registerSetting(settingModule{
+		Name:         "Hermes Gateway",
+		Desc:         "Start or stop the Hermes agent gateway on this box",
+		IconPath:     "img/joshu/chat.png",
+		Group:        "Joshu",
+		StartDir:     "SystemAO/joshu/hermes-gateway.html",
+		RequireAdmin: true,
+	})
+	registerSetting(settingModule{
+		Name:         "Box State",
+		Desc:         "Snapshot, restore, and factory reset for this Joshu box",
+		IconPath:     "img/joshu/chat.png",
+		Group:        "Joshu",
+		StartDir:     "SystemAO/joshu/box-state.html",
+		RequireAdmin: true,
+	})
+}
diff --git a/src/startup.go b/src/startup.go
index 2f3b8bc9..76dce430 100644
--- a/src/startup.go
+++ b/src/startup.go
@@ -96,6 +96,7 @@ func RunStartup() {
 	SystemIDInit()            //System UUID Manager
 	AuthSettingsInit()        //Authentication Settings Handler, must be start after user Handler
 	AdvanceSettingInit()      //System Advance Settings
+	JoshuSettingInit()        //Joshu box state (snapshots / factory)
 	StartupFlagsInit()        //System BootFlag settibg
 	HardwarePowerInit()       //Start host power manager
 	RegisterStorageSettings() //Storage Settings
diff --git a/src/system.go b/src/system.go
index 18575104..46c70f37 100644
--- a/src/system.go
+++ b/src/system.go
@@ -62,17 +62,6 @@ func SystemIDInit() {
 		StartDir: "SystemAO/info/srcLicense.html",
 	})
 
-	//Register vendor information
-	if fs.FileExists("web/SystemAO/vendor/index.html") {
-		registerSetting(settingModule{
-			Name:     "Vendor",
-			Desc:     "Vendor Notes",
-			IconPath: "SystemAO/info/img/small_icon.png",
-			Group:    "About",
-			StartDir: "SystemAO/vendor/index.html",
-		})
-	}
-
 	http.HandleFunc("/system/info/license", systemHandleListLicense)
 
 	//Handle health check ping
diff --git a/src/web/SystemAO/locale/system_settings.json b/src/web/SystemAO/locale/system_settings.json
index e9d947e5..7c658a5d 100644
--- a/src/web/SystemAO/locale/system_settings.json
+++ b/src/web/SystemAO/locale/system_settings.json
@@ -428,8 +428,9 @@
                 "menu/group/Security & Auth": "Security & Auth",
                 "menu/group/Developer Options": "Developer Options",
                 "menu/group/About ArOZ": "About ArozOS",
-                
-                "tab/info/Overview": "Overview",
+                "menu/group/Joshu": "Joshu",
+
+                "tab/joshu/Box State": "Box State",
                 "tab/info/Host Info": "Host Information",
                 "tab/info/Performance": "Performance",
                 "tab/info/Runtime": "Runtime",
@@ -483,10 +484,12 @@
                 "tab/about/Open Source": "Open Source Components",
                 "tab/about/License": "System License",
                 "tab/about/Vendor": "Vendor",
-                
+
                 "": ""
             },
             "titles": {
+                "Joshu box snapshots and factory profile": "Joshu box snapshots and factory profile",
+                "Snapshot, restore, and factory reset for this Joshu box": "Snapshot, restore, and factory reset for this Joshu box",
                 "Config and info about the Server Host": "Server Configuration and Information",
                 "Connected clients and IoT devices": "Clients and IoT Devices",
                 "List of modules loaded in the system": "System Modules and Subservices",
diff --git a/src/web/SystemAO/utilities/mediaPlayer.html b/src/web/SystemAO/utilities/mediaPlayer.html
index 412a64a4..12353540 100644
--- a/src/web/SystemAO/utilities/mediaPlayer.html
+++ b/src/web/SystemAO/utilities/mediaPlayer.html
@@ -46,6 +46,28 @@
 			z-index:99;
 			display:none;
 		}
+		#playPrompt{
+			position:absolute;
+			inset:0;
+			z-index:50;
+			display:flex;
+			align-items:center;
+			justify-content:center;
+			background:rgba(0,0,0,0.45);
+			cursor:pointer;
+		}
+		#playPrompt.hidden{
+			display:none;
+		}
+		#playPrompt .playLabel{
+			color:white;
+			font-size:1.1rem;
+			letter-spacing:0.04em;
+			padding:0.75rem 1.25rem;
+			border:1px solid rgba(255,255,255,0.35);
+			border-radius:4px;
+			background:rgba(0,0,0,0.35);
+		}
 		.blur{
 			filter: blur(2px);
 		}
@@ -59,7 +81,10 @@
 	</div>
 	<dib id="videocover" onclick="hideMenu();"></dib>
 	<div class="videoWrapper">
-		<video id="player" autoplay controls></video>
+		<div id="playPrompt" class="hidden" onclick="startPlayback();" title="Play video">
+			<span class="playLabel"><i class="play icon"></i> Play</span>
+		</div>
+		<video id="player" controls playsinline preload="none"></video>
 	</div>
 	<div id="mainmenu" class="ui left fixed inverted vertical menu" style="overflow-y:auto; width:220px;">
 		<div id="playerIcon" class="item">
@@ -74,14 +99,22 @@
 		var fileList = ao_module_loadInputFiles();
 		var vid = document.getElementById("player");
 		var currentPlaying = 0;
-		var targetVol = 0.7;
-		if (localStorage.getItem("global_volume") !== null && localStorage.getItem("global_volume") !== undefined){
-			targetVol = localStorage.getItem("global_volume");
+		var pendingPlay = null;
+
+		function resolveVolume(){
+			var raw = localStorage.getItem("global_volume");
+			var vol = parseFloat(raw);
+			if (isNaN(vol) || vol <= 0){
+				return 0.7;
+			}
+			return Math.min(1, vol);
 		}
 		
 		if (fileList !== null){
-			playFile(fileList[0]);
+			pendingPlay = fileList[0];
+			currentPlaying = 0;
 			ao_module_setWindowTitle(fileList[0].filename);
+			$("#playPrompt").removeClass("hidden");
 		}
 
 		$(".file.item").remove();
@@ -92,11 +125,28 @@
 		}
 
 		function playFileByID(id){
-			playFile(fileList[id]);
+			currentPlaying = id;
+			startPlayback(fileList[id]);
 			ao_module_setWindowTitle(fileList[id].filename);
 			hideMenu();
 		}
 
+		function startPlayback(fileObject){
+			var target = fileObject || pendingPlay;
+			if (target == null){
+				return;
+			}
+			pendingPlay = target;
+			$("#playPrompt").addClass("hidden");
+			playFile(target);
+			vid.muted = false;
+			vid.volume = resolveVolume();
+			var playAttempt = vid.play();
+			if (playAttempt && typeof playAttempt.catch === "function"){
+				playAttempt.catch(function(){});
+			}
+		}
+
 		function showMenu(){
 			$('.menu').show();
 			$('#videocover').show();
@@ -114,8 +164,11 @@
 				videoURL = '../../media/transcode?file=' + encodeURIComponent(fileObject.filepath);
 			}
 
-			$("#player").attr('src',videoURL);
-			vid.volume = parseFloat(targetVol);
+			vid.pause();
+			vid.removeAttribute("autoplay");
+			vid.muted = false;
+			$("#player").attr('src', videoURL);
+			vid.volume = resolveVolume();
 		}
 
 		function hideMenu(){
diff --git a/src/web/Video/embedded.html b/src/web/Video/embedded.html
index 53b6ed74..e3adfb89 100644
--- a/src/web/Video/embedded.html
+++ b/src/web/Video/embedded.html
@@ -39,10 +39,14 @@
             }
            
 
-            //Load global vol from localStorage
-            var defaultVol = localStorage.getItem("global_volume");
-            if (defaultVol == null || defaultVol == "" || defaultVol == undefined){
-                defaultVol = 0.4;
+            //Load global vol from localStorage (ignore zero — desktop used to default to 0)
+            function resolveVolume(){
+                var raw = localStorage.getItem("global_volume");
+                var vol = parseFloat(raw);
+                if (isNaN(vol) || vol <= 0){
+                    return 0.7;
+                }
+                return Math.min(1, vol);
             }
 
             function initPlayback(){
@@ -71,8 +75,8 @@
                 const dp = new DPlayer({
                     container: document.getElementById('dplayer'),
                     screenshot: true,
-                    autoplay: true,
-                    volume: parseFloat(defaultVol),
+                    autoplay: false,
+                    volume: resolveVolume(),
                     video: {
                         url: videoURL
                     },
diff --git a/src/web/desktop.html b/src/web/desktop.html
index 32478b70..02e184ca 100644
--- a/src/web/desktop.html
+++ b/src/web/desktop.html
@@ -20,7 +20,8 @@
             background-repeat: no-repeat;
             background-size: cover;
             background-position: center;
-            background-image: url('img/desktop/bg/init.jpg');
+            background-color: #000000;
+            background-image: none;
             width: 100%;
             height: 100%;
             overflow: hidden;
@@ -663,6 +664,13 @@
             width:100%;
             height: calc(100% - 28px);
             z-index:1114;
+            /* Default off: during jQuery fade the bar can sit at opacity:0, display:block
+               and still capture the whole viewport above the taskbar. */
+            pointer-events: none;
+        }
+
+        .notificationbar.jp-notifications-open{
+            pointer-events: auto;
         }
 
         .notificationbar .content{
@@ -703,6 +711,12 @@
             left:0;
             width:100%;
             height: calc(100%);
+            /* Default off: during jQuery fadeOut the cover can sit at opacity:0 but still
+               capture the full viewport (including the taskbar). Only enable when open. */
+            pointer-events: none;
+        }
+
+        .notificationbar.jp-notifications-open .cover{
             pointer-events: auto;
         }
 
@@ -1418,6 +1432,145 @@
         var isMacOS = (navigator.appVersion.indexOf("Mac")!=-1);
         var isTouchScreen = window.matchMedia("(any-pointer: coarse)").matches;
 
+        // Clear stuck full-screen layers (notification shade, drag capture planes, dimmed topmost windows).
+        function clearDesktopInitSplash() {
+            $("body").css({
+                "background-image": "none",
+                "background-color": "#000000"
+            });
+        }
+
+        function resetDesktopInteractionLayers() {
+            $(".notificationbar").removeClass("jp-notifications-open").stop(true, true).hide()
+                .css({"pointer-events": "none", "opacity": 1});
+            $(".notificationbar .cover").hide().css({"pointer-events": "none", "display": "none", "opacity": 0});
+            $("#fwdragpanel, #tfwdragpanel").hide().css({"pointer-events": "none"});
+            $('.floatWindow.topmost').css({"pointer-events": "auto", "opacity": 1});
+            $(".floatWindow").find(".iframecover").hide();
+            $("#selectionPanel").hide();
+            $("#quickAccessPanel").stop(true, true).hide();
+            $("#backgroundTaskPanel").stop(true, true).hide();
+            $("#listMenu").stop(true, true).hide();
+            $("body > .ui.dimmer.active").removeClass("active").hide();
+            movingWindow = false;
+            resizingWindow = false;
+            resizingEdgeID = 0;
+            multiSelecting = false;
+            multiSelectionStartPoint = [-1, -1];
+            resizingWindowTarget = undefined;
+            listMenuShown = false;
+        }
+
+        function scheduleDesktopLayerCleanup() {
+            // Never interrupt an intentional move/resize — timed cleanup is for stuck layers only.
+            [50, 200, 750, 2000, 5000, 10000].forEach(function(ms) {
+                window.setTimeout(function() {
+                    if (movingWindow || resizingWindow || multiSelecting) {
+                        return;
+                    }
+                    resetDesktopInteractionLayers();
+                }, ms);
+            });
+        }
+
+        // Focus the app iframe only when not mid-gesture (iframe focus blurs the desktop window).
+        function focusFloatWindowIframe(object) {
+            var iframe = $(object).find("iframe")[0];
+            if (!iframe || !iframe.contentWindow) {
+                return;
+            }
+            try {
+                iframe.contentWindow.focus();
+            } catch (ex) {
+                /* cross-origin or detached iframe */
+            }
+        }
+
+        function recoverDesktopInteraction() {
+            resetDesktopInteractionLayers();
+            $(".floatWindow").each(function() {
+                $(this).css({"pointer-events": "auto", "opacity": 1});
+                $(this).find(".iframecover").hide();
+            });
+            if (typeof hookFloatWindowEvents === "function") {
+                hookFloatWindowEvents();
+            }
+            if (typeof hookLaunchMenuEvents === "function") {
+                hookLaunchMenuEvents();
+            }
+            if (typeof moduleInstalled !== "undefined" && moduleInstalled.length > 0 && typeof listModulesType === "function") {
+                listModulesType("All");
+            } else if (typeof initModuleList === "function") {
+                initModuleList();
+            }
+            if (typeof refresh === "function") {
+                refresh(undefined, true);
+            }
+            try {
+                $(".dropdown").dropdown();
+            } catch (ex) {
+                /* ignore if Semantic UI not ready */
+            }
+        }
+
+        function markDesktopInitComplete() {
+            window.__arozDesktopInitComplete = true;
+            resetDesktopInteractionLayers();
+            scheduleDesktopLayerCleanup();
+            try {
+                var tabId = window.__arozDesktopTabId || "unknown";
+                localStorage.setItem("ao_desktop_ready", tabId + ":" + Date.now());
+            } catch (ex) {
+                /* ignore private mode / blocked storage */
+            }
+            if (typeof window.__arozOnDesktopInitComplete === "function") {
+                window.__arozOnDesktopInitComplete();
+            }
+        }
+
+        if (!window.__arozDesktopLifecycleHooks) {
+            window.__arozDesktopLifecycleHooks = true;
+            window.__arozDesktopTabId = Math.random().toString(36).slice(2);
+            window.resetDesktopInteractionLayers = resetDesktopInteractionLayers;
+            window.clearDesktopInitSplash = clearDesktopInitSplash;
+            clearDesktopInitSplash();
+            window.recoverDesktopInteraction = recoverDesktopInteraction;
+            window.markDesktopInitComplete = markDesktopInitComplete;
+            window.arozRecoverDesktop = function() {
+                if (!window.__arozDesktopInitComplete && typeof initDesktop === "function") {
+                    initDesktop();
+                } else {
+                    recoverDesktopInteraction();
+                }
+                if (typeof window.__arozDesktopDiag === "function") {
+                    console.log("[desktop] arozRecoverDesktop", window.__arozDesktopDiag());
+                }
+                return true;
+            };
+            // Register before async applocale init so bfcache pageshow is not missed.
+            window.addEventListener("pageshow", function(event) {
+                // Skip hard reset mid-gesture (bfcache restore during drag is rare but possible).
+                if (!(movingWindow || resizingWindow || multiSelecting)) {
+                    resetDesktopInteractionLayers();
+                }
+                scheduleDesktopLayerCleanup();
+            });
+            document.addEventListener("visibilitychange", function() {
+                if (document.visibilityState === "visible") {
+                    if (!(movingWindow || resizingWindow || multiSelecting)) {
+                        resetDesktopInteractionLayers();
+                    }
+                    scheduleDesktopLayerCleanup();
+                }
+            });
+            // Do NOT reset on window focus/blur while moving/resizing. Bring-to-front focuses the
+            // float-window iframe, which blurs the desktop window and previously aborted the gesture.
+            $(function() {
+                resetDesktopInteractionLayers();
+                scheduleDesktopLayerCleanup();
+            });
+        }
+
         //Check and prepare localization
         if (typeof(applocale) != "undefined"){
             //Applocale found. Do localization
@@ -1437,6 +1590,8 @@
 
 
         function initDesktop(){
+            resetDesktopInteractionLayers();
+            clearDesktopInitSplash();
             //Initiation functions
             initDesktopTheme();
             initDesktopHostInfo();
@@ -1461,6 +1616,8 @@
 
             //Activate all dropdowns
             $(".dropdown").dropdown();
+
+            markDesktopInitComplete();
         }
 
         function initFrameLoading(){
@@ -1717,7 +1874,7 @@
         }
 
         function showToolPanel(){
-            if (listMenuShown = true){
+            if (listMenuShown) {
                 hideListMenu();
             }
             $("#quickAccessPanel").fadeToggle("fast");
@@ -1739,7 +1896,7 @@
                     if (currentGlobalVol != null && currentGlobalVol != undefined && currentGlobalVol != ""){
                         
                     }else{
-                        currentGlobalVol = 0;
+                        currentGlobalVol = 0.7;
                     }
 
                     var audio = new Audio("media?file=" + data);
@@ -1798,7 +1955,7 @@
             if (currentGlobalVol != null && currentGlobalVol != undefined && currentGlobalVol != ""){
                 $("#volumebar").css("width", currentGlobalVol * 100 + "%");
             }else{
-                currentGlobalVol = 0;
+                currentGlobalVol = 0.7;
                 localStorage.setItem("global_volume",currentGlobalVol);
             }
 
@@ -1829,15 +1986,21 @@
         }
 
         function toggleNotification(action=undefined){
+            function syncNotificationOpenState() {
+                if ($(".notificationbar").is(":visible")) {
+                    $(".notificationbar").addClass("jp-notifications-open");
+                } else {
+                    $(".notificationbar").removeClass("jp-notifications-open");
+                }
+            }
             if (typeof action == "undefined"){
-                $(".notificationbar").stop().finish().fadeToggle("fast", function(){
-                  
-                });
+                $(".notificationbar").stop().finish().fadeToggle("fast", syncNotificationOpenState);
             }else{
                 if (action == "hide"){
-                    $(".notificationbar").stop().finish().fadeOut("fast");
+                    $(".notificationbar").removeClass("jp-notifications-open");
+                    $(".notificationbar").stop().finish().fadeOut("fast", syncNotificationOpenState);
                 }else if (action == "show"){
-                    $(".notificationbar").stop().finish().fadeIn("fast");
+                    $(".notificationbar").stop().finish().fadeIn("fast", syncNotificationOpenState);
                 }
             }
         }
@@ -2081,7 +2244,7 @@
                 fwmove($(this).parent(), evt);
 
                 //Add cusor change on resizble position reach
-                let hoverOffset = [event.pageX - $(this).offset().left, event.pageY - $(this).offset().top];
+                let hoverOffset = [evt.pageX - $(this).offset().left, evt.pageY - $(this).offset().top];
                 if (hoverOffset[1] <= 3 && !$(this).parent().hasClass("fixedsize")){
                     $(this).addClass("resizbleCursor");
                 }else{
@@ -2218,26 +2381,26 @@
                 if ($(object).hasClass("fixedsize")) {
                     return false;
                 }
-                var dx = event.pageX - $(object).find(".iframewrapper").offset().left;
-                var dy = event.pageY - $(object).offset().top;
+                var dx = evt.pageX - $(this).offset().left;
+                var dy = evt.pageY - $(object).offset().top;
+                // Scope cursor classes to this window only (upstream wiped every .iframewrapper).
+                var $wrap = $(this);
+                $wrap.removeClass("leftCorner rightCorner widthHover heighHover");
                 if (dx < 10 && dy > $(object).height() - 10) {
                     //Left Bottom Corner
-                    $(".iframewrapper").attr("class", "iframewrapper leftCorner");
+                    $wrap.addClass("leftCorner");
                 } else if (dx > $(object).width() - 10 && dy > $(object).height() - 10) {
                     //Right Bottom Corner
-                    $(".iframewrapper").attr("class", "iframewrapper rightCorner");
+                    $wrap.addClass("rightCorner");
                 } else if (dx < 10) {
                     //Dragging left edge
-                    $(".iframewrapper").attr("class", "iframewrapper widthHover");
+                    $wrap.addClass("widthHover");
                 } else if (dx > $(object).width() - 10) {
                     //Dragging right edge
-                    $(".iframewrapper").attr("class", "iframewrapper widthHover");
+                    $wrap.addClass("widthHover");
                 } else if (dy > $(object).height() - 10) {
                     //Bottom Edge
-                    $(".iframewrapper").attr("class", "iframewrapper heighHover");
-                } else {
-                    //???
-                    $(".iframewrapper").attr("class", "iframewrapper");
+                    $wrap.addClass("heighHover");
                 }
             });
 
@@ -2417,27 +2580,10 @@
                 return false;
             }
 
-            //Mouse down, start resizing
-            resizingWindow = true;
-            resizingWindowTarget = object;
-            resizingMinX = $(object).offset().left + $(object).width() - minxWidth;
-            MoveFloatWindowToTop(object);
-            if ($(object).hasClass("topmost")){
-                //Top most windows
-                $("#tfwdragpanel").show();
-            }else{
-                //Normal Windows
-                $('.floatWindow.topmost').css("pointer-events", "none");
-                $('.floatWindow.topmost').css("opacity", 0.7);
-                $("#fwdragpanel").show();
-            }
-          
-            
-            $(object).find(".iframecover").show();
-
-            //Check which edge is the user dragging
+            //Check which edge is the user dragging (before arming resize state)
             var dx = event.pageX - $(object).find(".iframewrapper").offset().left;
             var dy = event.pageY - $(object).offset().top;
+            resizingEdgeID = 0;
 
             if (dx < 10 && dy > $(object).height() - 10) {
                 //Left Bottom Corner
@@ -2461,6 +2607,30 @@
                 $(object).find(".iframewrapper").css('cursor', 's-resize');
             }
 
+            // Not on a resize edge — ignore (do not arm drag panels / covers).
+            if (resizingEdgeID == 0) {
+                return false;
+            }
+
+            //Mouse down, start resizing
+            resizingWindow = true;
+            resizingWindowTarget = object;
+            resizingMinX = $(object).offset().left + $(object).width() - minxWidth;
+            // Skip iframe focus during gesture — focusing blurs the desktop and breaks capture.
+            MoveFloatWindowToTop(object, { skipIframeFocus: true });
+            if ($(object).hasClass("topmost")){
+                //Top most windows
+                $("#tfwdragpanel").show().css({"pointer-events": "auto"});
+            }else{
+                //Normal Windows
+                $('.floatWindow.topmost').css("pointer-events", "none");
+                $('.floatWindow.topmost').css("opacity", 0.7);
+                $("#fwdragpanel").show().css({"pointer-events": "auto"});
+            }
+          
+            
+            $(object).find(".iframecover").show();
+
             //For upper edge, see fwdown() handler
         }
 
@@ -2519,8 +2689,8 @@
         function resizeUp(object, event) {
             resizingWindow = false;
             resizingEdgeID = 0;
-            $("#fwdragpanel").hide();
-            $("#tfwdragpanel").hide();
+            $("#fwdragpanel").hide().css({"pointer-events": "none"});
+            $("#tfwdragpanel").hide().css({"pointer-events": "none"});
             $('.floatWindow.topmost').css("pointer-events", "auto");
             $('.floatWindow.topmost').css("opacity", 1);
 
@@ -2536,6 +2706,7 @@
             resizingWindowTarget = undefined;
 
             $(".topmost").show();
+            focusFloatWindowIframe(object);
 
         }
 
@@ -2600,17 +2771,17 @@
             //Hide all contenxt menus
             hideAllContextMenus();
 
-            //Move the window to the top layer
-            MoveFloatWindowToTop(object);
+            //Move the window to the top layer (defer iframe focus until mouseup)
+            MoveFloatWindowToTop(object, { skipIframeFocus: true });
             
-            //Show the background drag panel
+            //Show the background drag panel (re-enable pointer-events — reset may have disabled them)
             if ($(object).hasClass("topmost")){
-                $("#tfwdragpanel").show();
+                $("#tfwdragpanel").show().css({"pointer-events": "auto"});
             }else{
                 
                 $('.floatWindow.topmost').css("pointer-events", "none");
                 $('.floatWindow.topmost').css("opacity", 0.7);
-                $("#fwdragpanel").show();
+                $("#fwdragpanel").show().css({"pointer-events": "auto"});
             }
             
             
@@ -2647,7 +2818,7 @@
                         height: originalSize[1],
                         left: newLeft,
                         right: "auto",
-                        top: event.Y - 2
+                        top: event.pageY - 2
                     });
                     clickDownOffset[0] = clickRatio * originalSize[0];
                     clickDownOffset[1] = 2;
@@ -2724,8 +2895,8 @@
         function fwup(object, event) {
             event = mapEventFromTouch(event);
             if (movingWindow) {
-                $("#fwdragpanel").hide();
-                $("#tfwdragpanel").hide();
+                $("#fwdragpanel").hide().css({"pointer-events": "none"});
+                $("#tfwdragpanel").hide().css({"pointer-events": "none"});
                 $('.floatWindow.topmost').css("pointer-events", "auto");
                 $('.floatWindow.topmost').css("opacity", 1);
 
@@ -2746,6 +2917,7 @@
 
                 $("#fwdockRightIndicator").hide();
                 $("#fwdockLeftIndicator").hide();
+                focusFloatWindowIframe(object);
             }else if (resizingWindow && resizingEdgeID == 6){
                 resizeUp($(object), event);
             }
@@ -2933,7 +3105,7 @@
             return false;
         }
 
-        function MoveFloatWindowToTop(object) {
+        function MoveFloatWindowToTop(object, opts) {
             var windowID = $(object).attr("windowId");
             var windowZindex = $(object).css("z-index");
             //Shift down all floatWindows that has z-index higher than this window by 2
@@ -3004,9 +3176,11 @@
                 $(object).fadeIn(100);
             }
 
-            //Focus on the top most floatWindow
-            var iframe = focusedWindow.find("iframe")[0];
-            iframe.contentWindow.focus();
+            // Focus the app iframe unless a move/resize gesture is in progress.
+            // Focusing mid-drag blurs the desktop window and historically aborted capture.
+            if (!(opts && opts.skipIframeFocus) && !movingWindow && !resizingWindow) {
+                focusFloatWindowIframe(object);
+            }
 
         }
 
@@ -3330,6 +3504,7 @@
 
         //Actiave background cross fade changing animation
         function initBackgroundSwitchingAnimation() {
+            clearDesktopInitSplash();
             if (localStorage.getItem("ao/desktop/backgroundInterval") !== null && !isNaN(parseInt(localStorage.getItem("ao/desktop/backgroundInterval")))){
                 //Localstorage has interval setting that is not NaN
                 backgroundCrossfadeInterval = parseInt(localStorage.getItem("ao/desktop/backgroundInterval")) * 1000;
@@ -3353,6 +3528,7 @@
 
         //Function for changing the desktop theme
         function changeDesktopTheme(targetThemeName) {
+            clearDesktopInitSplash();
             currentUserTheme = targetThemeName;
 
             if (currentUserTheme.includes(":/") == false){
@@ -3374,6 +3550,7 @@
                 }
                 setTimeout(function() {
                     $("#dbg0").addClass("showBackground");
+                    clearDesktopInitSplash();
                 }, 300);
 
                 if (currentBackgroundList.length == 1) {
@@ -3404,6 +3581,7 @@
                         }
                         setTimeout(function() {
                             $("#dbg0").addClass("showBackground");
+                            clearDesktopInitSplash();
                         }, 300);
 
                         if (currentBackgroundList.length == 1) {
@@ -3650,16 +3828,22 @@
                 endMultiSelection(event);
             }
 
-            if (resizingWindow){
-                //Resizing floatWindow out of the window bound (aka someone don't know how to resize a window correctly)
-                resizingWindow = false;
-                resizingEdgeID = 0;
-                $("#fwdragpanel").hide();
-                $("#tfwdragpanel").hide();
-                $('.floatWindow.topmost').css("pointer-events", "auto");
-                $('.floatWindow.topmost').css("opacity", 1);
-                $(resizingWindowTarget).find(".iframecover").hide();
-                $(resizingWindowTarget).find(".iframewrapper").css('cursor', '');
+            // End float-window gestures even when mouseup lands off the title bar / edge
+            // (e.g. over #fwdragpanel). Upstream only cleared resize, which left moves stuck.
+            if (movingWindow && focusedWindow !== undefined) {
+                fwup(focusedWindow, event);
+            } else if (resizingWindow) {
+                var resizeTarget = resizingWindowTarget !== undefined ? resizingWindowTarget : focusedWindow;
+                if (resizeTarget !== undefined) {
+                    resizeUp(resizeTarget, event);
+                } else {
+                    resizingWindow = false;
+                    resizingEdgeID = 0;
+                    $("#fwdragpanel").hide().css({"pointer-events": "none"});
+                    $("#tfwdragpanel").hide().css({"pointer-events": "none"});
+                    $('.floatWindow.topmost').css("pointer-events", "auto");
+                    $('.floatWindow.topmost').css("opacity", 1);
+                }
             }
             
         }
@@ -3804,9 +3988,9 @@
                 //Folder objects
                 icon = "folder outline";
                 if (isEmptyDir){
-                    imagePath = "img/desktop/system_icon/folder.png";
+                    imagePath = "img/joshu/folder.png?v=2";
                 }else{
-                    imagePath = "img/desktop/system_icon/folder-with-content.png";
+                    imagePath = "img/joshu/folder-open.png?v=2";
                 }
                 
                 type = "folder";
@@ -3904,7 +4088,8 @@
                 if (updateObject !== undefined){
                     //Check if content identical. If yes, skip rewrite
                     if (updateObject.attr("filedata") == compressedFiledata){
-                        //Identical. Skipping
+                        //Identical. Skipping layout rewrite but sync glyph path.
+                        updateObject.find(".launchIconImage").attr("src", imagePath);
                     }else{
                         //Update its contents
                         updateObject.attr("type", type);
@@ -3915,6 +4100,7 @@
                         updateObject.css("height", iheight + "px");
                         updateObject.css("left", screenX + "px");
                         updateObject.css("top", screenY + "px");
+                        updateObject.find(".launchIconImage").attr("src", imagePath);
                     }
                 }else{
                     //No exists. Append to wrapper
@@ -5031,13 +5217,7 @@
                 shiftHold = true;
             }else if (keycode == "27"){
                 //Cancel all floatwindow events and key events
-                $("#fwdragpanel").hide();
-                $("#tfwdragpanel").hide();
-                $(".floatWindow").find(".iframecover").hide();
-                movingWindow = false;
-                resizingWindow = false;
-                resizingEdgeID = 0;
-                multiSelecting = false;
+                resetDesktopInteractionLayers();
                 ctrlHold = false;
                 shiftHold = false;
             }else if (keycode == "46"){
@@ -5130,6 +5310,7 @@
             }else if (keycode == "27"){
                 //ESC key, stop all floatWindow operations
                 console.log("%c[Desktop] Dragging function killed. Restarting all dragging elements.", 'color: #e64747');
+                resetDesktopInteractionLayers();
                 movingWindow = false;
                 resizingWindow = false;
                 event.preventDefault();
@@ -6476,6 +6657,9 @@
                 //Find the correct file in the current directory and place its image
                 let thumbData = JSON.parse(event.data);
                 $(".launchIcon").each(function(){
+                    if ($(this).attr("type") === "folder") {
+                        return;
+                    }
                     if (thumbData[1].length > 0){
                         if ($(this).attr("filename") == thumbData[0]){
                             let imageContainer = $(this).find(".launchIconImage");
@@ -6517,6 +6701,9 @@
 
         function startFallbackThumbnailLoader(){
                 $(".launchIcon").each(function(){
+                    if ($(this).attr("type") === "folder") {
+                        return;
+                    }
                     let fd = JSON.parse(decodeURIComponent($(this).attr("filedata"))) 
                     let filepath = fd.Filepath;
                     let targetDOM = $(this);
